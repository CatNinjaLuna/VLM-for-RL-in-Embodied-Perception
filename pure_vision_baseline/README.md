# Vision-Language RL Agent for MiniGrid

**Authors:** Peiyao Tao, Carolina Li  
**Date:** 12/11/2025  
**Class:** CS 7180 Advanced Perception

# PPO Visual Baseline — MiniGrid 64×64 RGB

This experiment establishes a **pure-vision reinforcement learning (RL) baseline**
for the project _“Fine-Tuning Vision–Language Models for Reinforcement Learning in
Embodied Perception Tasks.”_

A **PPO agent** is trained on MiniGrid using only 64×64 RGB observations, providing
a controlled reference point for evaluating later Vision–Language and multimodal
policy variants.

---

## Environment Overview

-  **Environment:** `MiniGrid-Empty-8x8-v0`
-  **Observation:** 64×64 RGB frames (via `RGBImgPartialObsWrapper` + `ImgObsWrapper`)
-  **Action space:** Discrete (turn left/right, move forward)
-  **Reward:** +1 for reaching goal, 0 otherwise
-  **Training framework:** Stable-Baselines3 (PPO, CNN policy)
-  **Hardware:** CPU-only (Windows)

---

## Setup (Windows PowerShell)

```powershell
# Create and activate virtual environment
python -m venv .venv
.venv\Scripts\Activate

# Install dependencies
pip install gymnasium minigrid stable-baselines3==2.3.0 tensorboard matplotlib

# Run baseline training (10k steps quick test)
python baseline_minigrid_render.py --steps 10000 --n_envs 4 --save_frames

# View training logs
tensorboard --logdir ./logs/baseline --port 6006
```

---

## Output Structure

```
project_root/
│
├── baseline_minigrid_render.py       # main training script
├── models/
│   └── ppo_minigrid_baseline.zip     # saved PPO weights
├── samples/
│   └── frame_000.png ...             # rendered RGB frames
├── logs/
│   └── baseline/                     # TensorBoard logs
└── README.md

PPO_1, PPO_2, PPO_3:
  Distinct TensorBoard runs generated by Stable-Baselines3,
  corresponding to separate training executions (e.g., different seeds or restarts).


```

---

## Pretrained Models (Not Included)

Due to file size constraints, trained model checkpoints are **not included** in this repository.
All experiments described in this project were executed locally or on a computing cluster,
and the resulting model weights are stored offline.

The following checkpoints were produced during development and evaluation:

| Model File                             | Description                                                           |
| -------------------------------------- | --------------------------------------------------------------------- |
| `ppo_minigrid_baseline.zip`            | PPO visual-only baseline trained on MiniGrid-Empty-8x8                |
| `agent_phase1_goto.zip`                | Phase 1 agent: goal-reaching task without obstacles                   |
| `agent_phase3_ObstructedMaze_1d1h.zip` | Phase 3 agent: obstructed maze with VLM-assisted reasoning            |
| `agent_phase4_ObstructedMaze_2d1h.zip` | Phase 4 agent: harder obstructed maze with extended reasoning horizon |
| `minimap_doorkey_rewards.zip`          | MiniGrid DoorKey task with reward shaping                             |
| `rl_model_5615696_steps.zip`           | Long-horizon PPO training checkpoint (~5.6M steps)                    |

Model checkpoints are referenced here for **reproducibility and experiment tracking**.
All results reported in this README were obtained using these models.

---

## Training Metrics Summary (10k-Step Run)

| Metric               | Meaning                                           | Value / Interpretation                  |
| -------------------- | ------------------------------------------------- | --------------------------------------- |
| `fps`                | Frames processed per second                       | ~861 fps — efficient CPU training       |
| `approx_kl`          | KL divergence between new/old policies            | 0.0107 → stable updates                 |
| `entropy_loss`       | Policy randomness                                 | –1.94 → still exploring                 |
| `explained_variance` | Correlation between value predictions and returns | –0.95 → value network not learning yet  |
| `value_loss`         | MSE of value estimates                            | 0.0028 → low only because rewards are 0 |
| `avg_episode_length` | Steps before episode ends                         | 256 (max) → agent wanders without goal  |
| `avg_reward`         | Mean episode reward                               | 0.0 → no goal reached                   |
| `success_rate`       | Episodes completed successfully                   | 0.0 → expected early stage              |

---

## Interpretation

-  The model runs correctly but has not yet learned a meaningful policy.
-  10k steps are insufficient for PPO convergence on sparse, pixel-based MiniGrid tasks.
-  Negative `explained_variance` indicates the value network has not yet learned a
   useful return estimate.
-  Stable `approx_kl` and non-collapsing `entropy_loss` suggest healthy optimization
   dynamics and continued exploration.
-  This checkpoint serves as a functional baseline rather than a solved agent.
-  At this stage, the PPO policy behaves close to random, which is expected given
   the sparse reward structure and limited training horizon.

---

## Next Steps

1. **Longer training:** increase steps to 200k–500k for meaningful learning.

```bash
python baseline_minigrid_render.py --steps 500000 --n_envs 4
```

2. **Simpler environment:** try `MiniGrid-Empty-5x5-v0` for faster convergence.
3. **Analyze TensorBoard:** track reward and value loss curves.
4. **Prepare for VLM integration:** confirm that the 64×64 RGB pipeline is compatible
   with CLIP/BLIP-style visual embedding extraction.

---

## Expected Future Delivarables

-  Trained PPO baseline with non-zero success rate.
-  Reward curves and success-rate plots over training time.
-  Written baseline analysis summary (this document).
-  Saved model checkpoints for comparison with VLM-augmented variants.

---

## Demo & Presentation Videos

The following videos document qualitative agent behavior and project presentation.
They are provided to complement the quantitative results reported in this repository.

-  **Phase 3 Demo — Obstructed Maze Navigation (VLA Agent)**  
   https://youtu.be/Ou-NOhlSNDc  
   Demonstrates a Vision–Language–Action agent solving an obstructed MiniGrid maze
   using learned visual representations and goal-directed reasoning.

-  **Phase 4 Demo — Harder Obstructed Maze with Extended Reasoning Horizon**  
   https://youtu.be/iHWEXgXHQBU  
   Shows agent behavior in a more challenging maze configuration, highlighting
   improved robustness and multi-step decision-making.

-  **Final Project Presentation Video**  
   https://youtu.be/MmNIJl7Jxlc  
   Recorded presentation covering project motivation, baseline setup, VLA
   architecture, experimental phases, results, and conclusions.
